grim TestArray:
    init(elements):
        self.elements = elements
    
    spell append(val):
        self.elements += [val]
        return None

arr = TestArray([1, 2, 3])
print("Array created")
arr.append(4)
print("Append successful!")