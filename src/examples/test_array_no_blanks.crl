grim TestArray:
    init(elements):
        if elements == None:
            self.elements = []
        else:
            self.elements = elements
    spell append(val):
        self.elements += [val]
        return None
    spell length():
        return len(self.elements)

arr = TestArray([1, 2, 3])
print("Array created")
arr.append(4)
print("Append successful!")
print("Length:", arr.length())