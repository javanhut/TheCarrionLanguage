// Since string indexing isn't implemented, here are workarounds:

// Workaround 1: Convert string to array of characters
spell string_to_chars(s):
    // This would need to be implemented as a builtin
    // For now, let's demonstrate the concept
    return ["h", "e", "l", "l", "o"]  // Hardcoded for demo

// Workaround 2: Use string slicing if it were available
// Some languages have slice(string, start, end)

// Workaround 3: Use the String grim from stdlib
import "string"

s = String("hello world")
print("String length: " + str(s.length()))
print("Uppercase: " + s.upper())
print("Contains 'world': " + str(s.contains("world")))

// The String grim provides methods but not indexing
// You could add a char_at method to the String grim:
// spell char_at(index):
//     // Implementation would go here
//     return None