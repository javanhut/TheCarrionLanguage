grim SimpleArray:
    init(elements):
        self.elements = elements
    
    spell append(val):
        self.elements = self.elements + [val]
        return None
    
    spell length():
        return len(self.elements)