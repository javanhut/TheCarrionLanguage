arr = Array([1, 2, 3])
print("Initial array:", arr.to_string())
print("Array length:", arr.length())
print("Array is empty:", arr.is_empty())

arr.append(4)
print("After append(4):", arr.to_string())
print("New length:", arr.length())

print("Element at index 0:", arr.get(0))
print("Element at index 3:", arr.get(3))