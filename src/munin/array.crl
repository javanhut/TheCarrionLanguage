grim Array:
    init(elements):
        self.elements = []
        spell append(val):
            self.elements.append(val)
        spell length():
            return len(self.elements)
        spell to_string():
            return "[array]" // simple placeholder

    spell append(val):
        self.elements.append(val)

    spell length():
        return len(self.elements)

    spell to_string():
        return "[array]" // placeholder to avoid complex str
